export const shippingDetailsStyles = (theme => ({
    wrapper: {
        marginTop: theme.spacing(10),
    },
    autocompleteItem: {
        paddingLeft: theme.spacing(1),
        paddingRight: theme.spacing(1)
    }
}));