export const createAttributeStyles = (theme => ({
    buttonContainer: {
        display: 'flex',
        alignContent: 'center',
        justifyContent: 'center',
    },
    button: {
        margin: theme.spacing(3),
    },
}));